{
  "name": "grade",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "moon run web:dev",
    "build": "moon run web:build",
    "test": "moon run :test",
    "typecheck": "tsgo --noEmit -p apps/web/tsconfig.json && tsgo --noEmit -p packages/color/tsconfig.json && tsgo --noEmit -p packages/nodes/tsconfig.json && tsgo --noEmit -p packages/engine/tsconfig.json",
    "lint": "oxlint",
    "lint:fix": "oxlint --fix",
    "lint:type-aware": "oxlint --type-aware",
    "format": "oxfmt .",
    "format:check": "oxfmt --check .",
    "knip": "knip",
    "check": "pnpm run format:check && pnpm run lint && pnpm run typecheck"
  },
  "devDependencies": {
    "@typescript/native-preview": "^7.0.0-dev.20260621.1",
    "fast-check": "^4.8.0",
    "knip": "^6.17.1",
    "oxfmt": "^0.55.0",
    "oxlint": "^1.70.0",
    "oxlint-tsgolint": "^0.23.0"
  },
  "packageManager": "pnpm@10.33.0"
}
